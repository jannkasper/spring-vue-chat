# Database Configuration
spring.datasource.url=jdbc:postgresql://postgres:5432/chatdb
spring.datasource.username=chatuser
spring.datasource.password=chatpassword

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost,http://localhost:80,http://frontend:80
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# File Upload Configuration
app.file.upload-dir=/app/uploads 